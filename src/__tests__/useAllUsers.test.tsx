import { renderHook, act } from "@testing-library/react-hooks";
import userAllUsers from "../hooks/useAllUsers";

describe.skip("useAllUsers", () => {
  test("should render useAllUsers hook", async () => {
    // await act(() => renderHook(() => userAllUsers()));
    // console.log(result);
    // act(() => {
    //   result.current.
    // })
    // console.log(typeof result.current.users)
    // expect(typeof result.current.users).toBe('object')
  });
});
